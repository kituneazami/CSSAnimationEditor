# CSS Animation Editor - ユーザーガイド

## 目次
1. [クイックスタート](#クイックスタート)
2. [画面構成](#画面構成)
3. [基本操作](#基本操作)
4. [詳細機能](#詳細機能)
5. [キーボードショートカット](#キーボードショートカット)
6. [トラブルシューティング](#トラブルシューティング)

---

## クイックスタート

### 最速で始める（30秒）

1. **Quick Startボタンをクリック**
   - ヘッダー右上の「Quick Start」ボタンをクリック
   - サンプルのフェードインアニメーションが自動作成されます

2. **プレビューを見る**
   - 中央のプレビューエリアで青い四角が表示されます
   - 「Play」ボタンをクリックしてアニメーションを確認

3. **CSSコードをコピー**
   - 画面下部のコードビューアに生成されたCSSが表示されます
   - 「Copy」ボタンでクリップボードにコピー

### プリセットから始める（1分）

1. **Presetsボタンをクリック**
   - ヘッダーの「Presets」ボタンをクリック
   - プリセットライブラリが開きます

2. **好きなアニメーションを選択**
   - カテゴリから選ぶ：Entrance / Exit / Attention / Transform
   - プリセットカードの「Apply」ボタンをクリック

3. **再生して確認**
   - プレビューエリアの「Play」ボタンで再生
   - 気に入ったらコードをコピー

---

## 画面構成

```
┌─────────────────────────────────────────────────────────┐
│ ヘッダー                                                │
│ [Presets] [New] [Import] [Save] [Load] [Export]        │
├──────────┬──────────────────────────┬──────────────────┤
│ レイヤー │    プレビューエリア      │  プロパティ      │
│ パネル   │                          │  パネル          │
│          │  ┌────────────────────┐  │                  │
│ [+]      │  │                    │  │ Animation        │
│          │  │    アニメーション  │  │ Settings         │
│ Layer 1  │  │    プレビュー      │  │                  │
│  👁 🔒   │  │                    │  │ Keyframe         │
│          │  └────────────────────┘  │ Properties       │
│          │  [Play] [Stop] [Loop]   │                  │
├──────────┴──────────────────────────┴──────────────────┤
│ タイムラインエディタ                                    │
│ ├─────┼─────┼─────┼─────┼─────┤                      │
│ 0ms   250ms  500ms  750ms  1000ms                      │
├─────────────────────────────────────────────────────────┤
│ コードビューア                              [Copy]      │
│ @keyframes fadeIn { ... }                               │
└─────────────────────────────────────────────────────────┘
```

### 各エリアの説明

#### 1. ヘッダー
- **Presets**: プリセットアニメーションライブラリを開く
- **New Project**: 新しいプロジェクトを作成
- **Import**: プロジェクトファイル(.json)をインポート
- **Save**: ローカルストレージに保存
- **Load**: ローカルストレージから読み込み
- **Export**: プロジェクトファイル(.json)をダウンロード
- **Quick Start**: サンプルアニメーションを作成

#### 2. レイヤーパネル（左側）
- レイヤーの一覧を表示
- **+ボタン**: 新しいレイヤーを追加
- **👁アイコン**: レイヤーの表示/非表示
- **🔒アイコン**: レイヤーのロック/アンロック
- **↑↓ボタン**: レイヤーの順序変更
- **🗑ボタン**: レイヤーを削除

#### 3. プレビューエリア（中央上部）
- アニメーションのリアルタイムプレビュー
- **Play**: アニメーションを再生
- **Stop**: アニメーションを停止してリセット
- **Speed**: 再生速度を変更（0.25x〜2x）
- **Loop**: ループ再生のON/OFF
- **Grid**: グリッド表示のON/OFF

#### 4. タイムラインエディタ（中央下部）
- キーフレームを時間軸上に配置
- ドラッグ&ドロップでキーフレームを移動
- **Add Keyframe**: 新しいキーフレームを追加

#### 5. プロパティパネル（右側）
- **Animation Settings**: アニメーション全体の設定
  - Duration: 持続時間
  - Timing Function: イージング関数
  - Delay: 開始遅延
  - Iteration Count: 繰り返し回数
  - Direction: 再生方向
  - Fill Mode: 充填モード

- **Keyframe Properties**: 選択したキーフレームの設定
  - Offset: キーフレームの位置（0〜100%）
  - Opacity: 不透明度
  - Background Color: 背景色
  - Transform: 変形プロパティ
  - Filter: フィルター効果

#### 6. コードビューア（下部）
- 生成されたCSSコードを表示
- **Copy**: コードをクリップボードにコピー

---

## 基本操作

### 1. レイヤーの作成

**方法1: Quick Startを使う**
```
1. ヘッダーの「Quick Start」ボタンをクリック
2. 自動的にレイヤーとアニメーションが作成される
```

**方法2: 手動で作成**
```
1. レイヤーパネルの「+」ボタンをクリック
2. 新しいレイヤーが追加される
```

### 2. アニメーションの作成

**方法1: プリセットを使う**
```
1. 「Presets」ボタンをクリック
2. カテゴリを選択（All / Entrance / Exit / Attention / Transform）
3. 好きなプリセットの「Apply」ボタンをクリック
4. アニメーションが自動的に作成される
```

**方法2: 手動で作成**
```
1. レイヤーを選択
2. タイムラインの「Add Keyframe」ボタンをクリック
3. プロパティパネルでキーフレームの設定を編集
4. 複数のキーフレームを追加して動きを作る
```

### 3. キーフレームの編集

**キーフレームを選択**
```
1. タイムライン上のキーフレーム（◆マーク）をクリック
2. プロパティパネルに詳細が表示される
```

**プロパティを変更**
```
1. Offset: キーフレームの位置を変更
2. Opacity: 不透明度を設定
3. Background Color: カラーピッカーで色を選択
4. Transform: 変形プロパティを詳細設定
5. Filter: フィルター効果を適用
```

### 4. アニメーションの再生

**基本的な再生**
```
1. プレビューエリアの「Play」ボタンをクリック
2. アニメーションが再生される
3. 「Stop」ボタンで停止
```

**再生設定**
```
- Speed: ドロップダウンで速度を選択
- Loop: ボタンをクリックしてループON/OFF
```

### 5. CSSコードの取得

**コピー**
```
1. 画面下部のコードビューアを確認
2. 「Copy」ボタンをクリック
3. クリップボードにコードがコピーされる
```

**使い方**
```css
/* コピーしたコードをCSSファイルに貼り付け */
@keyframes fadeIn {
  0% { opacity: 0; }
  100% { opacity: 1; }
}

/* 要素に適用 */
.element {
  animation: fadeIn 1000ms ease-in-out;
}
```

### 6. プロジェクトの保存

**ローカルストレージに保存**
```
1. 「Save」ボタンをクリック
2. ブラウザのローカルストレージに保存される
3. 「Load」ボタンで復元可能
```

**ファイルとしてエクスポート**
```
1. 「Export」ボタンをクリック
2. JSONファイルがダウンロードされる
3. 「Import」ボタンで読み込み可能
```

---

## 詳細機能

### Transform（変形）プロパティ

キーフレームを選択すると、プロパティパネルに Transform エディタが表示されます。

**Translate（移動）**
```
- X: 横方向の移動（例: 100px, -50px）
- Y: 縦方向の移動（例: 50px, -100px）
- Z: 奥行き方向の移動（例: 20px）
```

**Rotate（回転）**
```
- Z: 平面回転（例: 45deg, 180deg）
- X: X軸回転（例: 90deg）
- Y: Y軸回転（例: 180deg）
```

**Scale（拡大縮小）**
```
- Uniform: 均等な拡大縮小（例: 1.5, 0.5）
- X: 横方向のみ（例: 2）
- Y: 縦方向のみ（例: 0.8）
```

**Skew（傾斜）**
```
- X: X軸方向の傾斜（例: 10deg）
- Y: Y軸方向の傾斜（例: 5deg）
```

### Filter（フィルター）プロパティ

**利用可能なフィルター**
```
- Blur: ぼかし効果（例: 5px）
- Brightness: 明るさ（例: 150%）
- Contrast: コントラスト（例: 120%）
- Grayscale: グレースケール（例: 100%）
- Hue Rotate: 色相回転（例: 90deg）
- Invert: 色反転（例: 100%）
- Saturate: 彩度（例: 200%）
- Sepia: セピア（例: 80%）
```

### プリセットアニメーション一覧

**Entrance（入場）**
- Fade In: フェードイン
- Slide In Left/Right/Up/Down: スライドイン
- Zoom In: ズームイン

**Exit（退場）**
- Fade Out: フェードアウト
- Slide Out Right: スライドアウト
- Zoom Out: ズームアウト

**Attention（注目）**
- Bounce: バウンス
- Pulse: パルス（拡大縮小）
- Shake: シェイク（揺れ）

**Transform（変形）**
- Rotate: 回転
- Flip: フリップ
- Circular Path: 円形パス（SVG）
- Wave Path: 波形パス（SVG）
- Infinity Path: ∞字パス（SVG）

### タイミング関数

**基本的なイージング**
- Linear: 等速
- Ease: なめらかな加減速
- Ease In: 加速
- Ease Out: 減速
- Ease In Out: 加速してから減速

**カスタムイージング**
- Ease In Quad/Cubic
- Ease Out Quad/Cubic
- Ease In Out Quad/Cubic

---

## キーボードショートカット

### 実装予定のショートカット

```
Ctrl + Z        アンドゥ（元に戻す）
Ctrl + Y        リドゥ（やり直す）
Ctrl + Shift + Z リドゥ（やり直す）

Ctrl + S        プロジェクトを保存
Ctrl + C        選択中の要素をコピー
Ctrl + V        ペースト

Space           アニメーション再生/一時停止
Delete          選択中のキーフレームを削除
Backspace       選択中のキーフレームを削除（Mac）

Esc             モーダルを閉じる
```

---

## トラブルシューティング

### アニメーションが再生されない

**確認事項**
1. レイヤーが表示状態か確認（👁アイコンがON）
2. タイムラインにキーフレームが2つ以上あるか確認
3. 「Play」ボタンがクリックされているか確認
4. Duration（持続時間）が0になっていないか確認

### コードが生成されない

**確認事項**
1. アニメーションが選択されているか確認
2. キーフレームが追加されているか確認
3. ブラウザのコンソールにエラーがないか確認

### プロジェクトが保存できない

**ローカルストレージ**
- ブラウザのローカルストレージが有効か確認
- プライベートブラウジングモードでは保存できません
- 代わりに「Export」でファイル保存してください

### レイヤーが表示されない

**確認事項**
1. レイヤーパネルで👁アイコンがONか確認
2. レイヤーのz-indexを確認
3. プレビューエリアをスクロールして確認

### パフォーマンスが悪い

**対処法**
1. レイヤー数を減らす
2. キーフレーム数を減らす
3. 複雑なフィルター効果を削除
4. ブラウザのハードウェアアクセラレーションを有効化

---

## ヒント＆Tips

### 効率的な作業方法

**1. プリセットをカスタマイズ**
```
1. プリセットを適用
2. キーフレームの位置を調整
3. プロパティを微調整
4. オリジナルアニメーションの完成
```

**2. レイヤーを活用**
```
1. 複数のレイヤーを作成
2. それぞれに異なるアニメーションを適用
3. タイミングをずらして動的な演出
```

**3. コードの再利用**
```
1. 気に入ったアニメーションをExport
2. JSONファイルとして保存
3. 別のプロジェクトでImport
```

### よくあるテクニック

**フェードイン＋スライド**
```
1. キーフレーム0%: opacity: 0, translateY: 50px
2. キーフレーム100%: opacity: 1, translateY: 0px
```

**バウンド効果**
```
1. キーフレーム0%: translateY: 0
2. キーフレーム25%: translateY: -30px
3. キーフレーム50%: translateY: 0
4. キーフレーム75%: translateY: -15px
5. キーフレーム100%: translateY: 0
```

**色の変化**
```
1. キーフレーム0%: backgroundColor: #3b82f6
2. キーフレーム50%: backgroundColor: #ef4444
3. キーフレーム100%: backgroundColor: #10b981
```

---

## サポート

### 問題が解決しない場合

1. ブラウザのキャッシュをクリア
2. ページをリロード
3. 別のブラウザで試す
4. GitHubリポジトリでIssueを作成

### 推奨環境

- **ブラウザ**: Chrome, Firefox, Safari, Edge（最新版）
- **Node.js**: 18以上（開発時）
- **画面解像度**: 1280x720以上

---

## さらに学ぶ

- [仕様書](./SPECIFICATION.md): 詳細な機能仕様
- [プロジェクト構造](./PROJECT_STRUCTURE.md): コード構造
- [チュートリアル](./TUTORIAL.md): ステップバイステップの学習

---

**Happy Animating! 🎨✨**
