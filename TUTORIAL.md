# CSS Animation Editor - チュートリアル

## はじめに

このチュートリアルでは、CSS Animation Editorの基本的な使い方から応用まで、実践的な例を通して学びます。

---

## チュートリアル1: 初めてのアニメーション作成（5分）

### 目標
シンプルなフェードインアニメーションを作成し、CSSコードを取得する

### ステップ

**1. アプリケーションを起動**
```bash
npm run dev
```
ブラウザで http://localhost:5173 を開く

**2. Quick Startを使う**
- 画面右上の「Quick Start」ボタンをクリック
- 自動的にレイヤーとアニメーションが作成されます

**3. アニメーションを確認**
- 中央のプレビューエリアに青い四角が表示されます
- 「Play」ボタンをクリックしてアニメーションを再生
- フェードインアニメーションが再生されます

**4. CSSコードを取得**
- 画面下部のコードビューアを確認
- 「Copy」ボタンをクリック
- 以下のようなコードがコピーされます：

```css
@keyframes fadeIn {
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
}

.animated-element {
  animation: fadeIn 1000ms ease-in-out;
}
```

**5. 完成！**
コピーしたコードを自分のプロジェクトで使用できます。

---

## チュートリアル2: プリセットを使ったアニメーション（5分）

### 目標
プリセットライブラリからアニメーションを選んで適用する

### ステップ

**1. プリセットライブラリを開く**
- ヘッダーの「Presets」ボタンをクリック
- モーダルウィンドウが開きます

**2. カテゴリを選択**
- 上部のカテゴリボタンから「Entrance」を選択
- 入場アニメーションの一覧が表示されます

**3. アニメーションを適用**
- 「Slide In Left」を探す
- 「Apply」ボタンをクリック
- モーダルが閉じて、アニメーションが作成されます

**4. 再生して確認**
- プレビューエリアの「Play」ボタンをクリック
- 左からスライドインする動きを確認

**5. 他のプリセットも試す**
- 「Presets」ボタンから再度開く
- 「Attention」カテゴリの「Bounce」を試す
- 「Transform」カテゴリの「Rotate」も試す

---

## チュートリアル3: カスタムアニメーションの作成（10分）

### 目標
手動でキーフレームを追加し、オリジナルのアニメーションを作成

### ステップ

**1. 新しいプロジェクトを作成**
- 「New Project」ボタンをクリック
- 確認ダイアログで「OK」

**2. レイヤーを追加**
- 左側のレイヤーパネルの「+」ボタンをクリック
- 「Layer 1」が作成されます

**3. 最初のキーフレームを追加**
- タイムラインの「Add Keyframe」ボタンをクリック
- キーフレームが50%の位置に追加されます

**4. キーフレームを編集**
- タイムライン上のキーフレーム（◆マーク）をクリック
- 右側のプロパティパネルで設定：
  - Offset: 0（開始位置）
  - Opacity: 0
  - Background Color: #3b82f6（青）

**5. 2つ目のキーフレームを追加**
- もう一度「Add Keyframe」ボタンをクリック
- 新しいキーフレームをクリックして選択
- プロパティを設定：
  - Offset: 100（終了位置）
  - Opacity: 1
  - Background Color: #ef4444（赤）

**6. Transformを追加**
- 1つ目のキーフレームを選択
- プロパティパネルの「Transform Properties」セクションまでスクロール
- Translate Y: 100px と入力

- 2つ目のキーフレームを選択
- Translate Y: 0px と入力

**7. アニメーション設定を調整**
- プロパティパネル上部の「Animation Settings」
- Duration: 2000 に変更（2秒）
- Timing Function: ease-in-out を選択

**8. 再生して確認**
- 「Play」ボタンをクリック
- 下から上にスライドしながらフェードインし、青から赤に色が変わる

**9. 完成！**
- CSSコードをコピーして使用

---

## チュートリアル4: 複雑なアニメーション（15分）

### 目標
複数のキーフレームを使って、バウンス効果のあるアニメーションを作成

### ステップ

**1. プリセットから開始**
- 「Presets」→「Attention」→「Bounce」を適用

**2. キーフレームを確認**
- タイムラインに複数のキーフレームが表示されます
- それぞれをクリックして、どんな設定か確認

**3. キーフレームをカスタマイズ**
- 最初のキーフレーム（0%）を選択
- Opacity: 0 を追加
- Background Color: #10b981（緑）

- 最後のキーフレーム（100%）を選択
- Opacity: 1 を追加
- Background Color: #f59e0b（オレンジ）

**4. 中間のキーフレームを調整**
- 25%のキーフレームを選択
- Transform の Translate Y を -40px に変更（より高くバウンス）

**5. Filterを追加**
- 0%のキーフレームを選択
- Filter Properties セクションまでスクロール
- Blur: 10px と入力

- 100%のキーフレームを選択
- Blur: 0px と入力

**6. アニメーション設定**
- Duration: 1500 に変更
- Timing Function: ease-out を選択
- Iteration Count: infinite（無限ループ）

**7. 再生**
- 「Loop」ボタンをONにする
- 「Play」ボタンをクリック
- バウンスしながらぼかしが消え、色が変化するアニメーション

---

## チュートリアル5: 複数レイヤーの使用（10分）

### 目標
複数のレイヤーを使って、時差アニメーションを作成

### ステップ

**1. 最初のレイヤーを作成**
- レイヤーパネルの「+」ボタンをクリック
- 「Presets」から「Slide In Left」を適用

**2. 2つ目のレイヤーを追加**
- もう一度「+」ボタンをクリック
- 新しいレイヤーが追加されます

**3. 2つ目のレイヤーにアニメーションを適用**
- レイヤー2を選択した状態で
- 「Presets」から「Slide In Right」を適用

**4. アニメーションのタイミングを調整**
- レイヤー2のアニメーションを選択
- Delay: 500 に設定（0.5秒遅れて開始）

**5. レイヤーの位置を調整**
- レイヤー1の要素スタイル:
  - プレビューで上に配置されるように調整

- レイヤー2の要素スタイル:
  - プレビューで下に配置されるように調整

**6. 3つ目のレイヤーを追加**
- さらに「+」ボタンをクリック
- 「Presets」から「Zoom In」を適用
- Delay: 1000 に設定

**7. 再生**
- 「Play」ボタンをクリック
- 3つの要素が順番にアニメーションする

**8. レイヤーの順序を変更**
- レイヤーパネルの↑↓ボタンで順序を入れ替え
- z-indexが変わり、重なり順が変化

---

## チュートリアル6: SVGパスアニメーション（10分）

### 目標
SVGパスに沿って要素を動かすアニメーションを作成

### ステップ

**1. プリセットライブラリを開く**
- 「Presets」ボタンをクリック
- 「Transform」カテゴリを選択

**2. Circular Pathを適用**
- 「Circular Path」プリセットを探す
- 「Apply」ボタンをクリック

**3. アニメーションを確認**
- 「Play」ボタンをクリック
- 要素が円形のパスに沿って動きます

**4. パスを変更**
- タイムラインのキーフレームを選択
- プロパティパネルで `offsetPath` を確認
- SVGパス文字列が表示されます

**5. 他のパスを試す**
- 「Presets」から「Wave Path」を適用
- 波形のパスに沿って動きます

- 「Infinity Path」も試す
- ∞字型のパスに沿って動きます

**6. パスアニメーションをカスタマイズ**
- Duration を変更して速度を調整
- Timing Function を linear にして等速に
- Direction を alternate にして往復させる

---

## チュートリアル7: プロジェクトの保存と共有（5分）

### 目標
作成したアニメーションを保存し、他の環境で使用する

### ステップ

**1. ローカルストレージに保存**
- 「Save」ボタンをクリック
- ブラウザのローカルストレージに保存されます
- メッセージは表示されませんが、正常に保存されています

**2. 保存したプロジェクトを読み込む**
- ページをリロード
- 「Load」ボタンをクリック
- 保存したプロジェクトが復元されます

**3. ファイルとしてエクスポート**
- 「Export」ボタンをクリック
- JSONファイルがダウンロードされます
- ファイル名: `プロジェクト名.json`

**4. エクスポートしたファイルをインポート**
- 「Import」ボタンをクリック
- ダウンロードしたJSONファイルを選択
- プロジェクトが読み込まれます

**5. 別の環境で使用**
- JSONファイルを別のPC/ブラウザに移動
- CSS Animation Editorで「Import」
- 同じプロジェクトが使用できます

**6. CSSコードの使用**
- コードビューアの「Copy」ボタンでコピー
- 自分のプロジェクトのCSSファイルに貼り付け
- HTMLで適用:

```html
<div class="animated-element">Hello!</div>
```

```css
/* コピーしたCSSを貼り付け */
@keyframes fadeIn {
  0% { opacity: 0; }
  100% { opacity: 1; }
}

.animated-element {
  animation: fadeIn 1000ms ease-in-out;
}
```

---

## チュートリアル8: 高度なテクニック（15分）

### 目標
Transform、Filter、複数プロパティを組み合わせた高度なアニメーション

### ステップ

**1. 新しいプロジェクトを作成**
- 「New Project」をクリック
- レイヤーを追加

**2. 複雑なTransformアニメーション**
- キーフレーム 0% を追加:
  - Opacity: 0
  - Transform:
    - Translate X: -100px
    - Translate Y: -100px
    - Rotate: -45deg
    - Scale: 0.5

- キーフレーム 50% を追加:
  - Opacity: 1
  - Transform:
    - Translate X: 0px
    - Translate Y: 0px
    - Rotate: 0deg
    - Scale: 1.2

- キーフレーム 100% を追加:
  - Opacity: 1
  - Transform:
    - Translate X: 0px
    - Translate Y: 0px
    - Rotate: 0deg
    - Scale: 1

**3. Filterを追加**
- キーフレーム 0%:
  - Blur: 10px
  - Brightness: 50%
  - Saturate: 0%

- キーフレーム 50%:
  - Blur: 5px
  - Brightness: 150%
  - Saturate: 200%

- キーフレーム 100%:
  - Blur: 0px
  - Brightness: 100%
  - Saturate: 100%

**4. アニメーション設定**
- Duration: 2000
- Timing Function: ease-in-out
- Direction: alternate
- Iteration Count: infinite

**5. 色の変化も追加**
- キーフレーム 0%: Background Color #3b82f6
- キーフレーム 50%: Background Color #ef4444
- キーフレーム 100%: Background Color #10b981

**6. 再生**
- 「Loop」をONにして「Play」
- 複雑で動的なアニメーションが再生されます

**7. コードを確認**
- コードビューアで生成されたCSSを確認
- 複雑なアニメーションも自動生成されます

---

## よくある質問

### Q: キーフレームの位置を変更するには？
A: プロパティパネルの「Offset」値を変更します。0〜100の範囲で設定できます。

### Q: アニメーションを無限ループにするには？
A: Animation Settings の「Iteration Count」の右にある「∞」ボタンをクリックします。

### Q: アニメーションを逆再生するには？
A: Direction を「reverse」または「alternate-reverse」に設定します。

### Q: 生成されたCSSがうまく動かない
A: ブラウザのプレフィックス（-webkit-など）が必要な場合があります。Autoprefixerなどのツールを使用してください。

### Q: 複数のアニメーションを1つの要素に適用できる？
A: CSSでは複数のアニメーションをカンマ区切りで指定できます：
```css
animation: fadeIn 1s, slideIn 1s;
```

---

## 次のステップ

1. **[ユーザーガイド](./USER_GUIDE.md)** で詳細機能を学ぶ
2. **[仕様書](./SPECIFICATION.md)** で技術的な詳細を確認
3. **独自のアニメーションライブラリ** を作成
4. **プリセットをカスタマイズ** してオリジナルを作成

---

**Happy Animating! 🎨✨**
